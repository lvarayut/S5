{% block user_block %}
    <ul class="nav navbar-nav pull-right"   >
        <li class="dropdown">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown">{{ app.session.get('username')|capitalize }}<b class="caret"></b></a>
            <ul class="dropdown-menu">
                <li><a href="{{ path('enstb_visplat_homepage') }}">Home</a></li>
                <li class="divider"></li>
                {% if is_granted('ROLE_ADMIN') %}
                    <li><a href="{{ path('enstb_data_management_import') }}">Import Dataset</a></li>
                    <li class="divider"></li>
                {% endif %}
                <li><a href="{{ path('logout') }}">Logout</a></li>
            </ul>
        </li>
    </ul>
{% endblock %}
